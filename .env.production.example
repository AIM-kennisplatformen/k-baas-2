# K-BAAS-2 Production Environment Variables
# This file contains non-secret production defaults and can be committed to git.
# SECRETS should be set in the deployment platform (e.g., Dokploy, Portainer)

# Docker Compose Port Mappings
FRONTEND_PORT=6166
BACKEND_PORT=6616
TYPEDB_PORT=1729
TYPEDB_HTTP_PORT=1728
AUTHENTIK_PORT_HTTP=9000
AUTHENTIK_PORT_HTTPS=9443

# Application Settings
APP_ENV=production
DEBUG=false

# Backend Configuration
BACKEND_HOST=0.0.0.0

# Frontend Configuration (update for your production domain)
VITE_BACKEND_URL=https://api.yourdomain.com

# TypeDB Database (host is container name in Docker)
TYPEDB_HOST=typedb
TYPEDB_DATABASE_NAME=kennisbaas
TYPEDB_SCHEMA_FILE=schema.tql
TYPEDB_SEED_FILE=seed.tql
TYPEDB_USER_NAME=admin
# Set to true to force database recreation and schema/seed reload on container restart
FORCE_REINIT=false

# Authentik Configuration (update for your production domain)
AUTHENTIK_PG_DB=authentik
AUTHENTIK_PG_USER=authentik
# Note: AUTHENTIK_URL is set in docker-compose.base.yml to use internal container hostname
# Do not override unless you have a specific networking requirement
AUTHENTIK_EXTERNAL_URL=https://auth.yourdomain.com
# AUTHENTIK_ISSUER must match the 'iss' claim in JWT tokens from Authentik
AUTHENTIK_ISSUER=https://auth.yourdomain.com/application/o/k-baas-2/
AUTHENTIK_APP_SLUG=k-baas-2

# Frontend Authentik URLs (update for your production domain)
# These use the EXTERNAL URL since they run in the user's browser
VITE_AUTHENTIK_URL=https://auth.yourdomain.com

# DO NOT SET SECRETS IN THIS FILE!
# Set these in your deployment platform:
# - AUTHENTIK_SECRET_KEY (required)
# - AUTHENTIK_PG_PASS (required)
# - AUTHENTIK_CLIENT_ID (from Authentik after setup)
# - AUTHENTIK_CLIENT_SECRET (from Authentik after setup)
# - VITE_AUTHENTIK_CLIENT_ID (same as AUTHENTIK_CLIENT_ID)
# - TYPEDB_ADMIN_PASSWORD
# - TYPEDB_USER_PASSWORD
# - JWT_SECRET_KEY
# - OPENAI_API_KEY (optional)
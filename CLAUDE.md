* CRUCIAL: Read AGENTS.md at the start of every session.

---

* Do not call `BashOutput` to read results of ordinary `Bash` tool runs. Only use `BashOutput` with a valid `shell_id` from a `Bash` command executed with `run_in_background: true`.